var React = require('react');
var ReactDOM = require('react-dom');
var SnapshotProvider = snapshotInteropRequire(require('react-snapshots/components/SnapshotProvider'));

<% testComponents.forEach(function(testComponent) { %>
  var <%= testComponent.name %> = snapshotInteropRequire(require('<%= testComponent.path %>'))
<% }); %>

function snapshotInteropRequire(mod) {
  return mod.__esModule ? mod.default : mod;
}

ReactDOM.render(
React.createElement(SnapshotProvider, {
  tests: [<%= testComponents.map(function() { return testComponent.name }).join(', ') %>]
}),
document.getElementById('root')
);
